{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Pizza Makers — сервер",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/server.js",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    },
    {
      "name": "Pizza Makers — тест бота",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/scripts/test-bot.js",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
